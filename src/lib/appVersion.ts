// Bumped by hand when we ship changes that matter to users (e.g. new
// backup schema, new modules). Lives here rather than package.json so it
// can be imported without Vite-specific JSON loading.
export const APP_VERSION = '0.1.0';
