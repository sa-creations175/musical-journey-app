/**
 * Unlocking Tiers of Difficulty — Section 3 of the Settings page.
 *
 * The shell only, in this commit: the section's own opening line, which
 * is the prototype's. The rule, the three ladders and the gate land in
 * the commit after this one.
 */
export default function UnlockingSection() {
  return (
    <p className="text-sm text-neutral-600 dark:text-neutral-300">
      In the Ear Training module, harder material is held back until the
      easier material is solid. Nothing is ever locked when you open a
      drill by hand; the Tiers only decide which chords, progressions and
      modes a practice session (or the quiz&apos;s own Next card) will
      serve you next.
    </p>
  );
}
