import ModuleCalendarPage from '../../components/ModuleCalendarPage';

/** The page every other module's calendar is now built from. */
export default function HarmonicFluencyCalendar() {
  return <ModuleCalendarPage moduleId="harmonic-fluency" />;
}
